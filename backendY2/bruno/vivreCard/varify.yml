info:
  name: varify
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseURL}}/auth/verify/{{verifyToken}}"
  body:
    type: json
    data: |-
      {
        "email": "tobak66551@cslua.com",
        "password" :"Azerty123"
      }
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
