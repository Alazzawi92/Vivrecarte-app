info:
  name: home-test-page
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseDeployedURL}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
