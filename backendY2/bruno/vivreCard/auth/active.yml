info:
  name: active
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}/users/active"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
