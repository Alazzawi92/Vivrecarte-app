info:
  name: login
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseDeployedURL}}/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "hicit34152@3dkai.com",
        "password" :"Azerty123"
      }
  auth:
    type: bearer
    token: ""

runtime:
  scripts:
    - type: after-response
      code: |-
        const token = res.getBody().token;
        bru.setVar("token",token)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
