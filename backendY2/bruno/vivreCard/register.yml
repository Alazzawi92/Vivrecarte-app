info:
  name: register
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseURL}}/auth/register"
  body:
    type: json
    data: |-
      {
        "email": "hicit34152@3dkai.com",
        "password": "Azerty123",
        "confirmPassword": "Azerty123"
       
      }
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
