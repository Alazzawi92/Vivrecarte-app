info:
  name: register
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseURL}}/auth/register"
  body:
    type: json
    data: |-
      {
        "email": "hicit34152@3dkai.com",
        "password" :"Azerty123",
         "confirmPassword" :"Azerty123"
      }
  auth:
    type: bearer
    token: ""

runtime:
  variables:
    - name: baseURL
      value: http://localhost:3000/api
    - name: DepolyedURL
      value: https://vivre-card.vercel.app/api

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
